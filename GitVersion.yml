mode: ContinuousDelivery
branches:
  master:
    tag: ""
    regex: ^master$|^main$
  feature:
    regex: ^feature?[/-]
  hotfix:
    tag: fix
    increment: Patch
    regex: ^(hot)?fix(es)?[/-]
ignore:
  sha: [ ]
merge-message-formats: { }