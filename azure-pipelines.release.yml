# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_release --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - release/*
  paths:
    exclude:
      - docs/*
      - README.md
      - CHANGELOG.md

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Tests
        displayName: 'Tests'
        dependsOn: [ Compile ]
        strategy:
          parallel: 3
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Tests --skip --test-partition $(System.JobPositionInPhase)'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: tests-results
              pathtoPublish: 'output/tests-results'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage-history
              pathtoPublish: 'output/coverage-history'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ Tests, Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'output/artifacts'
