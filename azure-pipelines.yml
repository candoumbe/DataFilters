# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - main
      - feature/*
      - fix/*
      - exp
  paths:
    exclude:
      - docs/*
      - README.md

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: SetupNuget
        displayName: 'SetupNuget'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd SetupNuget --skip'
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Test
        displayName: 'Test'
        dependsOn: [ Compile ]
        strategy:
          parallel: 10
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Test --skip --test-partition $(System.JobPositionInPhase)'
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: SetupNuget
        displayName: 'SetupNuget'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd SetupNuget --skip'
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Test
        displayName: 'Test'
        dependsOn: [ Compile ]
        strategy:
          parallel: 10
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Test --skip --test-partition $(System.JobPositionInPhase)'
