# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_pull-request --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - feature/*
      - hotfix/*
  paths:
    exclude:
      - docs/*
      - README.md
      - CHANGELOG.md

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Tests
        displayName: 'Tests'
        dependsOn: [ Compile ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Tests --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: tests-results
              pathtoPublish: 'output/tests-results'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage-history
              pathtoPublish: 'output/coverage-history'
