# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_pull-request --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pull-request

on:
  pull_request:
    branches:
      - develop
    paths:
      - '!docs/*'
      - '!README.md'
      - '!CHANGELOG.md'
      - '!LICENSE'

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', 'src/**/*.csproj') }}
      - name: Run './build.cmd Tests ReportCoverage'
        run: ./build.cmd Tests ReportCoverage
        env:
          CodecovToken: ${{ secrets.CODECOV_TOKEN }}
      - uses: actions/upload-artifact@v1
        with:
          name: tests-results
          path: output/tests-results
      - uses: actions/upload-artifact@v1
        with:
          name: coverage-report
          path: output/coverage-report
      - uses: actions/upload-artifact@v1
        with:
          name: coverage-history
          path: output/coverage-history
  macOS-latest:
    name: macOS-latest
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v1
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', 'src/**/*.csproj') }}
      - name: Run './build.cmd Tests ReportCoverage'
        run: ./build.cmd Tests ReportCoverage
        env:
          CodecovToken: ${{ secrets.CODECOV_TOKEN }}
      - uses: actions/upload-artifact@v1
        with:
          name: tests-results
          path: output/tests-results
      - uses: actions/upload-artifact@v1
        with:
          name: coverage-report
          path: output/coverage-report
      - uses: actions/upload-artifact@v1
        with:
          name: coverage-history
          path: output/coverage-history
